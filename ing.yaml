---
- hosts: localhost
  connection: local
  gather_facts: false
  user: root
  tasks:
    - name: provisoing a new hosts 
      ec2:
        key_name: mykey
        instance_type: t2.micro
        image: ami-123456
        wait: yes
        group: webserver
        count: 3
        vpc_subnet_id: subnet-29e632
